#include<bits/stdc++.h>
using namespace std;
//remove the rightmost set bit
int removeLastSetBit(int n)
{
   n  = (n) & (n-1);
  return n;
}
int main()
{
    cout<<"Number after toggling the ith bit is: "<<removeLastSetBit(40);
    return 0;
}
